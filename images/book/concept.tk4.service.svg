<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:data="http://www.data.org/1/data" width="1100" height="1800" version="1.1" viewBox="0 0 1100 1800">
  <defs>
    <radialGradient id="courtFill" cx="900" cy="200" r="125%" spreadMethod="pad" gradientUnits="userSpaceOnUse">
      <stop offset="0%" stop-color="#ffe166"/>
      <stop offset="100%" stop-color="#d69e48"/>
    </radialGradient>
  </defs>
  <g>
    <rect x="0" y="0" width="1100" height="1800" style="stroke: none; fill: #bdb5b7;"/>
    <rect x="200" y="200" width="700" height="1400" style="fill: url('#courtFill'); stroke: #0000b2; stroke-width: 5;"/>
    <line x1="200" y1="900" x2="900" y2="900" style="fill: none; stroke: #0000b2; stroke-width: 5;"/>
    <line x1="200" y1="1200" x2="900" y2="1200" style="fill: none; stroke: #0000b2; stroke-width: 5;"/>
    <line x1="200" y1="600" x2="900" y2="600" style="fill: none; stroke: #0000b2; stroke-width: 5;"/>
    <line x1="185" y1="1200" x2="35" y2="1200" style="fill: none; stroke: #0000b2; stroke-width: 5; stroke-dasharray: 15 15;"/>
    <line x1="915" y1="1200" x2="1065" y2="1200" style="fill: none; stroke: #0000b2; stroke-width: 5; stroke-dasharray: 15 15;"/>
    <line x1="185" y1="600" x2="35" y2="600" style="fill: none; stroke: #0000b2; stroke-width: 5; stroke-dasharray: 15 15;"/>
    <line x1="915" y1="600" x2="1065" y2="600" style="fill: none; stroke: #0000b2; stroke-width: 5; stroke-dasharray: 15 15;"/>
  </g>
  <g class="actors">
    <g transform="translate(150 1650)">
      <ellipse cx="0" cy="0" rx="65" ry="65" style="stroke: #696969; stroke-width: 5; fill: #808080;"/>
      <g id="ball_1" transform="translate(-29 -29) rotate(30)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_2" d="M -84 745.5 C -100.667 728.833 -109 709.25 -87.333 698 C -95.667 706.333 -92.333 725.5 -69 744 L -84 745.488 L -69 744 Q -76.5 747.167 -84 745.488" transform="translate(79 -721)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_3" d="M -58.339 735.167 C -70.839 722.667 -75.006 710.167 -77.339 702.667 Q -60.672 714.571 -54.006 721.833 Q -54.172 729 -58.353 735.167 Z" transform="translate(79 -721)" style="stroke: none; fill: #0000ff;"/>
      </g>
      <g id="ball_4" transform="translate(29 -29) rotate(-30)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_5" d="M -26 745.5 C -42.667 728.833 -51 709.25 -29.333 698 C -37.667 706.333 -34.333 725.5 -11 744 L -26 745.488 L -11 744 Q -18.5 747.167 -26 745.488" transform="translate(21 -721)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_6" d="M -0.339 735.167 C -12.839 722.667 -17.006 710.167 -19.339 702.667 Q -2.672 714.571 3.994 721.833 Q 3.828 729 -0.353 735.167 Z" transform="translate(21 -721)" style="stroke: none; fill: #0000ff;"/>
      </g>
      <g id="ball_7" transform="translate(29 29) rotate(60)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_8" d="M -26 803.5 C -42.667 786.833 -51 767.25 -29.333 756 C -37.667 764.333 -34.333 783.5 -11 802 L -26 803.488 L -11 802 Q -18.5 805.167 -26 803.488" transform="translate(21 -779)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_9" d="M -0.339 793.167 C -12.839 780.667 -17.006 768.167 -19.339 760.667 Q -2.672 772.571 3.994 779.833 Q 3.828 787 -0.353 793.167 Z" transform="translate(21 -779)" style="stroke: none; fill: #0000ff;"/>
      </g>
      <g id="ball_10" transform="translate(-29 29) rotate(-60)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_11" d="M -84 803.5 C -100.667 786.833 -109 767.25 -87.333 756 C -95.667 764.333 -92.333 783.5 -69 802 L -84 803.488 L -69 802 Q -76.5 805.167 -84 803.488" transform="translate(79 -779)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_12" d="M -58.339 793.167 C -70.839 780.667 -75.006 768.167 -77.339 760.667 Q -60.672 772.571 -54.006 779.833 Q -54.172 787 -58.353 793.167 Z" transform="translate(79 -779)" style="stroke: none; fill: #0000ff;"/>
      </g>
      <g id="ball_13" transform="translate(0 0) rotate(180)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_14" d="M -55 774.5 C -71.667 757.833 -80 738.25 -58.333 727 C -66.667 735.333 -63.333 754.5 -40 773 L -55 774.488 L -40 773 Q -47.5 776.167 -55 774.488" transform="translate(50 -750)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_15" d="M -29.339 764.167 C -41.839 751.667 -46.006 739.167 -48.339 731.667 Q -31.672 743.571 -25.006 750.833 Q -25.172 758 -29.353 764.167 Z" transform="translate(50 -750)" style="stroke: none; fill: #0000ff;"/>
      </g>
    </g>
    <g transform="translate(950 1650)">
      <ellipse cx="0" cy="0" rx="65" ry="65" style="stroke: #696969; stroke-width: 5; fill: #808080;"/>
      <g id="ball_17" transform="translate(-29 -29) rotate(30)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_18" d="M 716 745.5 C 699.333 728.833 691 709.25 712.667 698 C 704.333 706.333 707.667 725.5 731 744 L 716 745.488 L 731 744 Q 723.5 747.167 716 745.488" transform="translate(-721 -721)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_19" d="M 741.661 735.167 C 729.161 722.667 724.994 710.167 722.661 702.667 Q 739.328 714.571 745.994 721.833 Q 745.828 729 741.647 735.167 Z" transform="translate(-721 -721)" style="stroke: none; fill: #0000ff;"/>
      </g>
      <g id="ball_20" transform="translate(29 -29) rotate(-30)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_21" d="M 774 745.5 C 757.333 728.833 749 709.25 770.667 698 C 762.333 706.333 765.667 725.5 789 744 L 774 745.488 L 789 744 Q 781.5 747.167 774 745.488" transform="translate(-779 -721)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_22" d="M 799.661 735.167 C 787.161 722.667 782.994 710.167 780.661 702.667 Q 797.328 714.571 803.994 721.833 Q 803.828 729 799.647 735.167 Z" transform="translate(-779 -721)" style="stroke: none; fill: #0000ff;"/>
      </g>
      <g id="ball_23" transform="translate(29 29) rotate(60)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_24" d="M 774 803.5 C 757.333 786.833 749 767.25 770.667 756 C 762.333 764.333 765.667 783.5 789 802 L 774 803.488 L 789 802 Q 781.5 805.167 774 803.488" transform="translate(-779 -779)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_25" d="M 799.661 793.167 C 787.161 780.667 782.994 768.167 780.661 760.667 Q 797.328 772.571 803.994 779.833 Q 803.828 787 799.647 793.167 Z" transform="translate(-779 -779)" style="stroke: none; fill: #0000ff;"/>
      </g>
      <g id="ball_26" transform="translate(-29 29) rotate(-60)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_27" d="M 716 803.5 C 699.333 786.833 691 767.25 712.667 756 C 704.333 764.333 707.667 783.5 731 802 L 716 803.488 L 731 802 Q 723.5 805.167 716 803.488" transform="translate(-721 -779)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_28" d="M 741.661 793.167 C 729.161 780.667 724.994 768.167 722.661 760.667 Q 739.328 772.571 745.994 779.833 Q 745.828 787 741.647 793.167 Z" transform="translate(-721 -779)" style="stroke: none; fill: #0000ff;"/>
      </g>
      <g id="ball_29" transform="translate(0 0) rotate(180)">
        <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
        <path id="path_30" d="M 745 774.5 C 728.333 757.833 720 738.25 741.667 727 C 733.333 735.333 736.667 754.5 760 773 L 745 774.488 L 760 773 Q 752.5 776.167 745 774.488" transform="translate(-750 -750)" style="stroke: none; fill: #0000ff;"/>
        <path id="path_31" d="M 770.661 764.167 C 758.161 751.667 753.994 739.167 751.661 731.667 Q 768.328 743.571 774.994 750.833 Q 774.828 758 770.647 764.167 Z" transform="translate(-750 -750)" style="stroke: none; fill: #0000ff;"/>
      </g>
    </g>
    <line x1="0" y1="0" x2="0" y2="-700" transform="translate(550 900)" style="fill: none; stroke: #0000b2; stroke-width: 3;"/>
    <g class="player attack" id="player_34" transform="translate(550 1650)">
      <ellipse cx="0" cy="0" rx="50" ry="25" style="stroke: none; fill: #b22222;"/>
      <g class="prepare-block">
        <line x1="-37.5" y1="-25" x2="-22.5" y2="-25" style="fill: none; stroke: #b22222; stroke-width: 12.5; stroke-linecap: round; stroke-linejoin: round;"/>
        <line x1="22.5" y1="-25" x2="37.5" y2="-25" style="fill: none; stroke: #b22222; stroke-width: 12.5; stroke-linecap: round; stroke-linejoin: round;"/>
      </g>
      <g class="block">
        <line x1="-41" y1="-25" x2="41" y2="-25" style="fill: none; stroke: #b22222; stroke-width: 12.5; stroke-linecap: round; stroke-linejoin: round;"/>
      </g>
      <g class="prepare-defense">
        <line x1="-37.5" y1="0" x2="-62.5" y2="-25" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
        <line x1="-62.5" y1="-25" x2="-62.5" y2="-50" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
        <line x1="37.5" y1="0" x2="62.5" y2="-25" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
        <line x1="62.5" y1="-25" x2="62.5" y2="-50" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
      </g>
      <g class="defense">
        <line x1="-41" y1="0" x2="0" y2="-75" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
        <line x1="41" y1="0" x2="0" y2="-75" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
      </g>
      <g class="attack">
        <line x1="37.5" y1="0" x2="37.5" y2="-50" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
      </g>
      <g class="attack-left">
        <line x1="-37.5" y1="0" x2="-37.5" y2="-50" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
      </g>
      <g class="set">
        <line x1="-37.5" y1="0" x2="-20" y2="-25" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
        <line x1="37.5" y1="0" x2="20" y2="-25" style="fill: none; stroke: #b22222; stroke-width: 17; stroke-linecap: round; stroke-linejoin: round;"/>
      </g>
    </g>
    <g class="move">
      <g transform="translate(578.056 1535.807)">
        <line x1="0" y1="0" x2="-191.056" y2="-1050.807" style="stroke: #000000; stroke-width: 1; stroke-linecap: round; stroke-linejoin: round; stroke-dasharray: 2 4;"/>
        <polyline points="-200.248,-1023.725 -191.056,-1050.807 -172.919,-1028.694" style="stroke: #000000; stroke-width: 1; stroke-linecap: round; stroke-linejoin: round; stroke-dasharray: 2 4; fill: none;"/>
      </g>
      <g transform="translate(592.868 1535.346)">
        <line x1="0" y1="0" x2="124.132" y2="-1050.346" style="stroke: #000000; stroke-width: 1; stroke-linecap: round; stroke-linejoin: round; stroke-dasharray: 2 4;"/>
        <polyline points="107.405,-1027.148 124.132,-1050.346 134.991,-1023.888" style="stroke: #000000; stroke-width: 1; stroke-linecap: round; stroke-linejoin: round; stroke-dasharray: 2 4; fill: none;"/>
      </g>
    </g>
    <g id="ball_35" transform="translate(587 1585)">
      <ellipse cx="0" cy="0" rx="25" ry="25" style="stroke: none; fill: #ffff00;"/>
      <path id="path_36" d="M 382 709.5 C 365.333 692.833 357 673.25 378.667 662 C 370.333 670.333 373.667 689.5 397 708 L 382 709.488 L 397 708 Q 389.5 711.167 382 709.488" transform="translate(-387 -685)" style="stroke: none; fill: #0000ff;"/>
      <path id="path_37" d="M 407.661 699.167 C 395.161 686.667 390.994 674.167 388.661 666.667 Q 405.328 678.571 411.994 685.833 Q 411.828 693 407.647 699.167 Z" transform="translate(-387 -685)" style="stroke: none; fill: #0000ff;"/>
    </g>
  </g>
  <!-- context menu - - - - - - - - - - -->
  <rect id="contextmenu_bg" x="0" y="0" />
  <g id="contextmenu" class="contextmenu">
    <rect x="0" y="0" width="204" height="100" rx="5" ry="5" />
    <g id="ID_animation" class="ID_mi_stop">
    <g class="menuitem" data-item="ID_mi_play" transform="translate(0 0)">
     <rect x="2" y="2" width="200" height="32" class="bg" />
     <path d="M 12 10 l 7 7 l -7 7 z" class="mi-play" />
     <text x="30" y="25">Start</text>
    </g>
    <g class="menuitem" data-item="ID_mi_pause" transform="translate(0 32)">
     <rect x="2" y="2" width="200" height="32" class="bg" />
     <path d="M 12 10 v 14 M 18 10 v 14" class="mi-pause" />
     <text x="30" y="25">Pause</text>
    </g>
    <g class="menuitem" data-item="ID_mi_stop" transform="translate(0 64)">
     <rect x="2" y="2" width="200" height="32" class="bg" />
     <path d="M 8 10 h 14 v 14 h -14 z" class="mi-stop" />
     <text x="30" y="25">Stop</text>
    </g>
    </g>
  </g>
  <style>
#contextmenu_bg {
  width: 100%;
  height: 100%;
  fill: #7777777f;
}
#contextmenu_bg, #contextmenu {
  display: none;
  cursor: default;
}

g.contextmenu > rect, g.submenu > rect {
  stroke: gray;
  stroke-width: 3px;
  fill: lightgray;
}

g.menuitem > rect, g.submenuitem > rect {
  stroke: none;
  fill: lightgray;
  /*cursor: pointer;*/
}
g.menuitem > * {
  pointer-events: none;
}
g.menuitem > rect.bg {
  pointer-events: all;
  cursor: pointer;
}
g.submenuitem > rect.bg {
  /*pointer-events: all;*/
}
g.menuitem:hover > rect.bg, g.submenuitem:hover > rect.bg {
  fill: darkgray;
}
g.menuitem > text, g.submenuitem > text {
  stroke: none;
  fill: #111;
  font-family: Arial;
  font-size: 22px;
  /*cursor: pointer;*/
}
/*
g.submenuitem > rect, g.submenuitem > text {
  cursor: default;
}
*/
g.menuitem > rect.cb, g.menuitem > circle.rb {
  stroke: gray;
  stroke-width: 2px;
  fill: none;
}
g.menuitem > circle.rb2 {
  stroke: none;
  fill: #111;
}

g.menuitem > path.cb2, g.submenuitem > path.marker_submenu {
  stroke: #111;
  stroke-width: 2px;
  fill: none;
  display: block;
}
g.submenu {
  display: none;
}
g.submenuitem:hover > g.submenu {
  display: block;
}

#contextmenu > rect.separator {
  fill: #BBB;
  stroke: none;
}


g.menuitem > path.mi-play, g.menuitem > path.mi-stop {
  stroke: none;
  fill: #444;
}
g.menuitem > path.mi-pause {
  stroke: #444;
  stroke-width: 3px;
  fill: none;
}


g.actors.animation > g.move {
  display: none !important;
}
g.player > g {
  display: none;
}
g.player.attack > g.attack {
  display: block;
}
g.player.attack-left > g.attack-left {
  display: block;
}
g.player.prepare-block > g.prepare-block {
  display: block;
}
g.player.block > g.block {
  display: block;
}
g.player.prepare-defense > g.prepare-defense {
  display: block;
}
g.player.defense > g.defense {
  display: block;
}
g.player.set > g.set {
  display: block;
}

g#ID_animation.ID_mi_stop > g[data-item="ID_mi_stop"],
g#ID_animation.ID_mi_stop > g[data-item="ID_mi_pause"] {
  opacity: 0.5;
}
g#ID_animation.ID_mi_play > g[data-item="ID_mi_play"] {
  opacity: 0.5;
}
g#ID_animation.ID_mi_pause > g[data-item="ID_mi_pause"] {
  opacity: 0.5;
}
g#ID_animation.ID_mi_stop > g[data-item="ID_mi_stop"]:hover > text,
g#ID_animation.ID_mi_stop > g[data-item="ID_mi_pause"]:hover > text {
  cursor: default;
}
g#ID_animation.ID_mi_stop > g[data-item="ID_mi_stop"]:hover > rect,
g#ID_animation.ID_mi_stop > g[data-item="ID_mi_pause"]:hover > rect {
  cursor: default;
}
  </style>
<script>//<![CDATA[
(function(){
/**
 * Selects the first matching dom element.
 * @param {string} selector The selector of the element.
 * @param {DOMElement} parent The optional root of the search (default: document).
 * @returns DOMElement The first matching DOM element.
 */
function $(selector, parent) {
  return (parent ? parent : document).querySelector(selector);
}

/**
 * Selects DOMElements.
 * @param {string} selector The selector of the DOMElements.
 * @param {DOMElement} parent The optional root of the search (default: document).
 * @returns {NodeList} The list of matching DOMElements.
 */
function $$(selector, parent) {
  return (parent ? parent : document).querySelectorAll(selector);
}

/**
 * Connects an event handler to a given DOMElement.
 * @param {DOMElement} elem The DOMElement to handle.
 * @param {string} event The name of the event to handle.
 * @param {Function} handler The event handler.
 * @returns {void}
 */
function $event(elem, event, handler) {
  elem.addEventListener(event, handler);
}
//-- create menu script -------------------------------------- (vvv) --
// --- ios helper --------------------------------- vv ---
var timer = null,
    cmPosX = -1000,
    cmPosY = -1000;

function touchstart(e) {
  cmPosX = e.touches[0].clientX;
  cmPosY = e.touches[0].clientY;
  touchend();
  if (e.touches.length === 1) {
    timer = setTimeout(function() { onlongtouch(cmPosX, cmPosY); }, 800);
  }
}

function touchmove(e) {
  if (timer) {
    if (Math.abs(cmPosX - e.touches[0].clientX) > 5 ||
        Math.abs(cmPosY - e.touches[0].clientY) > 5) {
      touchend();
    }
  }
}

function touchend() {
  if (timer) {
    clearTimeout(timer);
    timer = null;
  }
}

function onlongtouch(x,y){
  timer = null;
  showContextMenu(x, y);
}

$event(document, 'touchstart', touchstart);
$event(document, 'touchend', touchend);
$event(document, 'touchmove', touchmove);
// --- ios helper --------------------------------- ^^ ---/* global $$ */
var cm, bg,
    contextmenu = false;

function onContextMenu(event) {
  event.preventDefault();

  // ignore clicks outside of svg area
  var svg = _getSvgImg();
  if (!svg || !svg.contains(event.target)) {
    return;
  }

  // InternalError is unique for firefox
  //var isFirefox = !!window.InternalError,
  //    x = isFirefox ? event.clientX : event.pageX,
  //    y = isFirefox ? event.clientY : event.pageY;
  showContextMenu(event.clientX, event.clientY);
}

function _getSvgImg() {
  var svg = $('div.img > svg');
  if (!svg) {
    svg = $('svg');
  }
  return svg;
}

function showContextMenu(x, y) {
  // show context menu if not visible
  if (!contextmenu) {
    contextmenu = true;

    /**
     * Switch an option state.
     * @param {NodeList} elems A list of html elements.
     * @returns {void}
     */
    var setOpts = function(elems) {
      if (elems) {
        for (var i = 0, i2 = elems.length; i < i2; ++i) {
          var elem = elems[i],
              elemP = elem.parentNode;

          if (elemP.dataset && elemP.dataset.item) {
            var key = elemP.dataset.item;
            if (option.data[key] === true || option.data[key] === false) {
              elem.style.display = option.data[key] ? 'block' : 'none';
     	    }
          }
        }
      }
    };

    // handle all options: radios and checkboxes
    var rbs = document.getElementsByClassName('rb2'),
        cbs = document.getElementsByClassName('cb2');
    setOpts(rbs);
    setOpts(cbs);

    // show background + context menu
    cm.style.display = 'block';
    bg.style.display = 'block';

    //var svg = document.getElementsByTagName('svg')[0], pt, svgP;
    var svg = _getSvgImg(), pt, svgP;
    try {
      // transform point to SVG coordinates
      pt = new DOMPoint(x,y);
      svgP = pt.matrixTransform(svg.getScreenCTM().inverse());
    } catch {
      // old browsers: create SVG point, set pos, transform to SVG coordinates
      pt = svg.createSVGPoint();
      pt.x = x;
      pt.y = y;
      svgP = pt.matrixTransform(svg.getScreenCTM().inverse());
    }

    cm.setAttribute('transform', 'translate(' + svgP.x + ' ' + svgP.y + ') scale(1.65)');
  }
}

function onClick(event) {
  if (contextmenu) {
    contextmenu = false;
    cm.style.display = 'none';
    bg.style.display = 'none';

    var node = event.target;
    while (node) {
      if (node.dataset && node.dataset.item) {
        if (node.parentNode
            && node.parentNode.parentNode
            && node.parentNode.parentNode.dataset
            && node.parentNode.parentNode.dataset.group) {
          option.reset(node.parentNode.parentNode.dataset.group);
        }

        option.set(node.dataset.item);
        node = null;
      } else {
        node = node.parentNode;
      }
    }
  }
}

var option = {
  data: {

  },

  // the current state info
  _current: {
  },

  // TODO animation, effects
  // the connected animation info
  'animator': undefined,
  'animation0': undefined,
  // the connected effects handler
  effects: null,

  reset: function(keys) {
    if (typeof keys === 'string') {
      var keys2 = keys.split(',');
      if (Array.isArray(keys2)) {
        for (var i = 0, i2 = keys2.length; i < i2; ++i) {
          this.data[keys2[i]] = false;
          this._updateElems(keys2[i], false);
        }
      }
    }
  },

  set: function(key) {
    this.data[key] = !this.data[key];
    this._updateElems(key);
  },

  init: function() {
    for (var key in this.data) {
      console.log('_updateElems ' + key);
      
      this._updateElems(key);
    }
  },

  getCurrent: function(id) {
    if (this._current[id] !== undefined) {
      return this._current[id];
    }
    return true;
  },

  _updateElems: function(key, state) {
    var st = state === undefined ? this.data[key] : state;

    // items that reflect the state
    var elems = $$('[data-option="' + key + '"]');
    if (elems) {
      for (var i = 0, i2 = elems.length; i < i2; ++i) {
        var elem = elems[i];
        elem.style.display = st ? "block" : "none";
        if (elem.id) {
          this._current[elem.id] = st;
        }
      }
    }

    // items that reflect the inverted state
    elems = $$('[data-option="' + key + '_not"]');
    if (elems) {
      for (var i = 0, i2 = elems.length; i < i2; ++i) {
        var elem = elems[i];
        elem.style.display = st ? "none" : "block";
        if (elem.id) {
          this._current[elem.id] = !st;
        }
      }
    }
  }
};

option.init();

cm = $('#contextmenu');
bg = $('#contextmenu_bg');
$event(document, 'contextmenu', onContextMenu);
$event(bg, 'click', onClick);
$event(cm, 'click', onClick);

// init size of context submenu items (because of text size)
$$('g.submenuitem').forEach((sub) => {
  $event(sub, 'mouseenter', () => {
    var txts = $$('g.submenu > g.menuitem > text');
    // 200 size of menu item + 2px width border, 30 is left of text field
    var len = 170;
    txts.forEach((txt) => len = Math.max(len, Math.ceil(txt.getBBox().width + 5)));
    if (len > 170) {
      var first = true;
      txts.forEach((txt) => {
        if (first) {
          var rc = $('rect', txt.parentNode.parentNode);
          rc.style.width = (len + 34) + 'px';
          first = false;
        }
        var rc = $('rect', txt.parentNode);
        rc.style.width = (len + 30) + 'px';
      });
    }
  });
});

//-- create menu script -------------------------------------- (^^^) --
var animation0 = {
  "player_34": {
    "playertype": "player attack"
  },
  "ball_35": {
    "x": 587.0,
    "y": 1585.0,
    "angle": 0.0,
    "scale": 1.0,
    "visible-common": true
  }
};
var animation = {
  "player_34": [
    {
      "type": "pla-typ",
      "playertype": "player",
      "at": "-1.0"
    },
    {
      "type": "pla-typ",
      "playertype": "player attack",
      "at": "1.9"
    },
    {
      "type": "pla-typ",
      "playertype": "player",
      "at": "2.3"
    },
    {
      "type": "pla-typ",
      "playertype": "player attack",
      "at": "5.9"
    },
    {
      "type": "pla-typ",
      "playertype": "player",
      "at": "6.3"
    }
  ],
  "ball_35": [
    {
      "type": "mov",
      "x": -37.0,
      "y": 40.0,
      "mode": "linear",
      "start": -1.0,
      "end": -1.0
    },
    {
      "type": "mov",
      "x": 37.0,
      "y": -40.0,
      "mode": "linear",
      "start": 1.0,
      "end": 2.0
    },
    {
      "type": "mov",
      "x": -200.0,
      "y": -1100.0,
      "mode": "linear",
      "start": 2.0,
      "end": 3.5
    },
    {
      "type": "mov",
      "x": 163.0,
      "y": 1140.0,
      "mode": "linear",
      "start": 3.6,
      "end": 3.6
    },
    {
      "type": "mov",
      "x": 37.0,
      "y": -40.0,
      "mode": "linear",
      "start": 5.0,
      "end": 6.0
    },
    {
      "type": "mov",
      "x": 130.0,
      "y": -1100.0,
      "mode": "linear",
      "start": 6.0,
      "end": 7.5
    },
    {
      "type": "scale",
      "scale": 0.5,
      "start": 1.0,
      "end": 1.55
    },
    {
      "type": "scale",
      "scale": -0.5,
      "start": 1.55,
      "end": 2.0
    },
    {
      "type": "scale",
      "scale": 0.5,
      "start": 5.0,
      "end": 5.55
    },
    {
      "type": "scale",
      "scale": -0.5,
      "start": 5.55,
      "end": 6.0
    },
    {
      "type": "vis",
      "visible": false,
      "at": 3.5
    },
    {
      "type": "vis",
      "visible": true,
      "at": 4.0
    },
    {
      "type": "vis",
      "visible": false,
      "at": 7.5
    }
  ]
};
/* global $$, animation, animation0 */

var animator = {
  // to reset all animations
  animation0: null,
  // the steps of the animations
  animation: null,

  // the duration of the animation
  duration: 0,
  // when to restart looping
  restart: 0,

  // the animation timer id
  ID: undefined,
  // the current timestamp of the animation
  ts: 0,
  // the last system timestamp to calculate the next step
  lastTs: 0,

  // the connected effects handler
  effects: null,
  // the connected options handler
  option: null,

  /**
   * Initializes the animations - sets the data.
   * @param {array} animation0 How to reset all animations to restart.
   * @param {array} animation The animations steps.
   * @returns {void}
   */
  init: function(animation0, animation) {
    this.animation0 = animation0;
    this.animation = animation;
  },

  /**
   * Initializes the animations - sets the times.
   * @param {number} duration The duration of the animation.
   * @param {number} restart When to restart looping.
   * @returns {void}
   */
  initAnimation: function(duration, restart) {
    this.duration = duration;
    this.restart = restart;
  },

  isActive: function() {
    return !!this.ID;
  },

  start: function() {
    if (!this.ID) {
      this.ID = setInterval(this.handler.bind(this), 33);
    }
    this.lastTs = Date.now();
  },
  pause: function() {
    if (this.ID) {
      clearInterval(this.ID);
      this.ID = undefined;
    }
  },
  stop: function() {
    if (this.ID) {
      clearInterval(this.ID);
      this.ID = undefined;
    }
    this.ts = 0;
    this._reset(true);
  },

  handler: function() {
    // calculate next step
    var cur = Date.now();
    this.ts += (cur - this.lastTs) / 1000;
    this.lastTs = cur;

    // handle looping
    while (this.ts > this.duration) {
      this.ts -= this.duration - this.restart;
    }

    // calculate animation and update
    this._reset(false);
    this._update(this.ts);
  },

  _reset: function(full) {
    var keys = Object.keys(this.animation0);
    for (var i = 0; i < keys.length; ++i) {
      var id = keys[i],
          anim0 = this.animation0[id];

      this._setActor(id, anim0);
      if (full && anim0.draw !== undefined) {
        // console.log("reset draw " + keys[i]);
        this._setDraw(keys[i], anim0.draw);
      }
    }
  },

  _update: function(time) {
    // handle each actor
    var keys = Object.keys(this.animation0);
    for (var i = 0; i < keys.length; ++i) {
      var id = keys[i],
          anim0 = this.animation0[id],
          anims = this.animation[id];
      // ignore anim0 without animations (e.g. visibility without any animation)
      if (!anims) {
        continue;
      }
      var cur = {},
          keys2 = Object.keys(anim0);

      // clone anim0 (=initial pos/angle/...)
      for (var j = 0; j < keys2.length; ++j) {
        cur[keys2[j]] = anim0[keys2[j]];
      }

      // handle each animation of this actor
      for (var a = 0; a < anims.length; ++a) {
        var anim = anims[a];

        switch (anim.type) {
          case 'mov':
            if (time >= anim.end) {
              cur.x += anim.x;
              cur.y += anim.y;

            } else if (time > anim.start) {
              var dur = anim.end - anim.start,
                  t = time - anim.start;

              switch (anim.mode) {
                case "linear":
                  cur.x += anim.x / dur * t;
                  cur.y += anim.y / dur * t;
                  break;

                case "parable":
                  cur.x += this._getX(anim.angle, anim.speed, t / anim.slowmo);
                  cur.y += this._getY(anim.angle, anim.speed, t / anim.slowmo);
                  break;

                default:
                  log('Invalid move mode: ' + anim.mode + '!');
              }
            }
            break;

          case 'rot':
            if (time >= anim.end) {
              cur.angle += anim.angle;

            } else if (time > anim.start) {
              var dur = anim.end - anim.start,
                  t = time - anim.start;
              cur.angle += anim.angle / dur * t;
            }
            break;

          case 'scale':
            if (time >= anim.end) {
              cur.scale += anim.scale;
            } else if (time > anim.start) {
              var dur = anim.end - anim.start,
                  t = time - anim.start;
              cur.scale += anim.scale / dur * t;
            }
            break;

          case 'vis':
            if (time >= anim.at) {
              // udate right styling variant
              var postfix = !!this.effects && this.effects.current[id] ? this.effects.current[id] : 'common';
              cur['visible-' + postfix] = anim.visible;
            }
            break;

          case 'pla-typ':
            if (time >= anim.at) {
              cur.playertype = anim.playertype;
            }
            break;

          case 'pla-txt':
            if (time >= anim.at) {
              cur.playertext = anim.playertext;
            }
            break;

          case 'draw':
            // nothing to change for main item -> cur
            // subitems are updated in setDraw(...)
            // console.log("update draw");
            this._setDraw(id, anim0.draw, anim.items, time);
            break;

          default:
            console.log('Unsupported animation type: ' + anim.type + '!');
            break;
        }
      }

      this._setActor(id, cur);
    }
  },

  _setActor: function(id, anim) {
    const actor = $('#' + id);
    if (!actor) return;

    var keys = Object.keys(anim),
        trans = '';
    for (var i = 0; i < keys.length; ++i) {
      switch (keys[i]) {
        case 'x':
          trans += this._setPos(anim.x, anim.y);
          break;
        case 'y':
          // nothing to do
          break;

        case 'angle':
          trans += this._setRot(anim.angle, anim.rotx, anim.roty);
          break;
        case 'rotx':
        case 'roty':
          // nothing to do
          break;

        case 'scale':
          trans += this._setScale(anim.scale, anim.rotx, anim.roty);
          break;

        case 'playertype':
          this._setPlayerType(actor, anim.playertype);
          break;

        case 'playertext':
          this._setPlayerText(actor, anim.playertext);
          break;

        case 'draw':
          // nothing to do
          break;

        // nur animation
        // case 'visible':
        //  this._setVisibility(actor, anim.visible);
        //  break;
        // option handling ?!
        // effects handling


        default:
          var found = false;
          var parts = keys[i].split('-');
          if (parts.length === 2) {
            switch (parts[0]) {
              case 'visible':
                found = true;
                var cur = !!this.effects && this.effects.current[id] ? this.effects.current[id] : 'common';
                if (cur === parts[1]) {
                  this._setVisibility(actor, anim['visible-' + cur]);
                }
                break;
            }
          }
          if (!found) {
            console.log('Unsupported animation property: ' + keys[i] + '-' + anim[keys[i]] + '!');
          }
      }
    }
    if (trans !== '') {
      actor.setAttribute('transform', trans.trim());
    }
  },

  _setPos: function(x, y) {
    return ' translate(' + x + ', ' + y + ')';
  },

  _setRot: function(angle, rotx, roty) {
    return (rotx !== undefined && roty !== undefined)
        ? (' rotate(' + angle + ', ' + rotx + ', ' + roty + ')')
        : (' rotate(' + angle + ')');
  },

  _setScale: function(scale, cx, cy) {
    return (cx !== undefined && cy !== undefined)
        ? ' translate(' + cx + ', ' + cy + ') scale(' + scale + ') translate(' + -cx + ', ' + -cy + ')'
        : ' scale(' + scale + ')';
  },

  _setVisibility: function(actor, visible) {
    actor.style.display = visible ? 'block' : 'none';
  },

  _setPlayerType: function(actor, playertype) {
    actor.setAttribute('class', playertype);
  },

  _setPlayerText: function(actor, text) {
    var texts = actor.getElementsByTagName('text');
    if (texts !== null && texts.length > 0) {
      texts[0].innerHTML = text;
    }
  },

  _getX: function(angle, speed, time) {
    return speed * Math.cos(angle) * time * 1000;
  },

  _getY: function(angle, speed, time) {
    var G2 = 9.81 / 2.0;
    return -(speed * Math.sin(angle) * time - G2 * time * time) * 1000;
  },

  _setDraw: function(id, pos0, anims, time) {
    // initialize positions and check
    var pos1 = JSON.parse(JSON.stringify(pos0));
    if (!Array.isArray(pos1)) {
      log("Invalid animation data for " + id + "!");
      return;
    }

    // handle each position (if in animation)
    if (anims && time) {
      // check
      if (!Array.isArray(anims) || pos1.length !== anims.length) {
        log("Invalid animation data for " + id + "!");
        return;
      }

      // handle each position
      for (var p = 0, p2 = pos1.length; p < p2; ++p) {
        var pos = pos1[p],
            angle = pos1[p].angle,
            length = pos1[p].length;

        // handle each animation of position
        for (var a = 0, a2 = anims[p].length; a < a2; ++a) {
          var ai = anims[p][a];

          if (ai.end <= time) {
            switch (ai.type) {
              case 'show':
                pos.visible = ai.visible;
                break;

              case 'xy':
                pos.dx += ai.dx;
                pos.dy += ai.dy;
                break;

              case 'rel':
                angle += ai.angle;
                length += ai.length;
                var aa = angle * Math.PI / 180;
                pos.dx = Math.cos(aa) * length;
                pos.dy = Math.sin(aa) * length;
                break;
            }
          } else if (ai.start < time) {
            var dur = time - ai.start,
                duration = ai.end - ai.start;
            switch (ai.type) {
              case 'xy':
                pos.dx += ai.dx / duration * dur;
                pos.dy += ai.dy / duration * dur;
                break;

              case 'rel':
                angle += ai.angle / duration * dur;
                length += ai.length / duration * dur;
                var aa = angle * Math.PI / 180;
                pos.dx = Math.cos(aa) * length;
                pos.dy = Math.sin(aa) * length;
                break;
            }
          }
        }
      }
    }

    // connect positions (0 cannot have any predecessor)
    for (var p = 1, p2 = pos1.length; p < p2; ++p) {
      var pos = pos1[p];
      if (pos.predecessor >= 0) {
        var posP = pos1[pos.predecessor];
        pos.dx += posP.dx;
        pos.dy += posP.dy;
      }
    }

    // set items depending on the positions
    $$('g#' + id + ' > [data-anim-index]').forEach((item) => {
      var idx = parseInt(item.dataset.animIndex);
      if (isFinite(idx)) {
        var pos = pos1[idx],
            posP = pos.predecessor >= 0 ? pos1[pos.predecessor] : null;

        // set visibility
        this._setVisibility(item, pos.visible);

        // set position
        switch (item.tagName.toLowerCase()) {
          //<line x1="107" y1="115" x2="159" y2="115" id="draw_1_20" data-anim-index="9" style="fill: none; stroke: #111111; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round;"/>
          case 'line':
            if (posP !== null) {
              item.setAttribute('x1', posP.dx);
              item.setAttribute('y1', posP.dy);
              item.setAttribute('x2', pos.dx);
              item.setAttribute('y2', pos.dy);
            } else {
              log("Cannot set line info for " + idx + "!");
            }
            break;

          //<ellipse cx="154" cy="107" rx="8" ry="8" id="draw_1_22" data-anim-index="10" style="stroke: none; fill: #eeeeee;"/>
          case 'ellipse':
            item.setAttribute('cx', pos.dx);
            item.setAttribute('cy', pos.dy);
            break;

          default:
            console.error('Cannot set position for svg-draw-item ' + item.tagname + '!');
        }
      }
    });
  }
};
animator.init(animation0, animation);

function onAnimClick(event) {
  var elem = event.target;
  while (elem) {
    if (elem.dataset && elem.dataset.item) {
      switch (elem.dataset.item) {
        case 'ID_mi_play':
          $$('g.actors').forEach((actors) => actors.className.baseVal = 'actors animation');
          animator.start();
          $('#ID_animation').className.baseVal = elem.dataset.item;
          break;
        case 'ID_mi_pause':
          // 'pause' is not allowed if state is 'stopped'
          if ($('#ID_animation').className.baseVal !== "ID_mi_stop") {
            animator.pause();
            $('#ID_animation').className.baseVal = elem.dataset.item;
          }
          break;
        case 'ID_mi_stop':
          $$('g.actors').forEach((actors) => actors.className.baseVal = 'actors scratch');
          animator.stop();
          $('#ID_animation').className.baseVal = elem.dataset.item;
          break;
      }
      elem = null;
    } else {
      elem = elem.parentNode;
    }
  }
}

$event($('#ID_animation'), 'click', onAnimClick);
animator.initAnimation(8.0, 0.0);
}());
//]]></script>
</svg>

